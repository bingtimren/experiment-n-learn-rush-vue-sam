{
  "name": "sam-app",
  "description": "test sam web backend in monorepo",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "aws-sdk": "^2.799.0",
    "@bingsjs/greeting": "~1.0.0-alpha.0"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  },
  "scripts": {
    "build": "echo nothing to build",
    "test": "echo nothing to test"
  }
}
